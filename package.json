{
  "name": "buildit-proposals",
  "version": "1.0.0",
  "description": "BuiltIt. Proposal Generator — standalone Node.js app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "pip install reportlab || echo 'Python not found, skipping pip install'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "pptxgenjs": "^4.0.1"
  }
}
