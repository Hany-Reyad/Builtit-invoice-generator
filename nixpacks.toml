[phases.setup]
nixPkgs = ["...", "python3", "python311Packages.pip"]

[phases.build]
cmds = ["npm install", "pip install reportlab"]

[staticAssets]
# Forces Railway to include these specific files in the deployment container
include = ["index.html", "invoice.html", "invoice_generator.py", "generator.js"]
